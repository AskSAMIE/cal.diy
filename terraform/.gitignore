# Local Terraform working dirs / state — never commit these.
.terraform/
*.tfstate
*.tfstate.*
crash.log
*.tfvars.local
override.tf
override.tf.json
*_override.tf
# DO commit .terraform.lock.hcl (provider pinning).
